meta {
  name: Delete order again
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/orders/{{orderId}}
  body: none
  auth: inherit
}

tests {
  test("returns 404", function() {
    expect(res.getStatus()).to.equal(404);
  });
  test("explains the error", function() {
    expect(res.getBody().detail).to.equal("Order not found");
  });
}
