meta {
  name: Get item
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/items/{{itemId}}
  body: none
  auth: inherit
}

tests {
  test("returns 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
}
